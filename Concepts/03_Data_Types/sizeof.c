#include <stdio.h>

int main()
{
    int a = 0;
    printf("%d", sizeof(a));
    return 0;
    /*In this part we have seen the role of 'sizeof' operator.
    The output is the number of bytes any datatype takes.
    'sizeof' is an uninary operator not a function.*/

    
}